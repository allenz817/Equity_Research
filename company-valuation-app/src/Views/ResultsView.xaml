<Window x:Class="CompanyValuationApp.Views.ResultsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:CompanyValuationApp.ViewModels"
        Title="Valuation Results" Height="450" Width="800">
    <Window.DataContext>
        <vm:ValuationResultViewModel />
    </Window.DataContext>
    <Grid>
        <TextBlock Text="Valuation Results" FontSize="24" FontWeight="Bold" Margin="10" />
        <ListView ItemsSource="{Binding ValuationResults}" Margin="10,50,10,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Metric" DisplayMemberBinding="{Binding Metric}" Width="200"/>
                    <GridViewColumn Header="Value" DisplayMemberBinding="{Binding Value}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Export Report" Command="{Binding ExportReportCommand}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10"/>
    </Grid>
</Window>